# Создадим матрицу сигнатурных пар **патоген-ген**

## Объединим результаты работы BLAST, KRAKEN2 и SAMtools inxstat

Для этого будем использовать ID контигов и ОРС

```{python}
import pandas as pd
import os
from tqdm import tqdm
import time
import re

#определим функции
def drop_gene_number_from_scaffold_name(x): #модификация названия -> оставляем только имя cкаффолда
    return (str(x).split("$")[0])

def modify_genus_name(x): #уберем (taxid #) из названия таксона
    return (str(x).split(' (')[0])

def get_gene_name(x): #берем имя гена после $ в sseqid из VFDB
    return (str(x).split("$")[1])


```

```{python}
#| eval: false

pattern = r".*\.csv$"
regex = re.compile(pattern)
directory = 'Blast-results/Blast-results-filtered'

dfs = []
for filename in tqdm(os.listdir(directory), desc = 'tqdm() Progress Bar'):
    time.sleep(0.5)
    if regex.match(filename):
      print(f"\n{filename}\n")
      filepath = os.path.join(directory, filename)  
      patient = filename.split('file_')[1].split('.')[0]
      
      #Читаем результат Blast
      blast = pd.read_csv(filepath, sep='\t')[['orf', 'sseqid','pident']]
      blast['Gene'] = blast['sseqid'].apply(get_gene_name)

      rc = pd.read_csv(f'Readcounts/Readcount-results/Readcount_result_{patient}.txt',sep="\t",header=None,
                     names=['orf','sequence length','readcounts','unmapped read-segments'],dtype={'readcount': 'Int64'})[['orf','readcounts']]
      #удаляем у RC * c конца
      rc = rc.iloc[:-1,:]
      
      #Объединяем RC и Bl по названию гена в скаффолде
      rc_blast = rc.merge(blast, how='inner', on='orf')
      rc_blast['Contig'] = rc_blast['orf'].apply(drop_gene_number_from_scaffold_name)
      #суммируем ридкаунты для одинаковых генов в скаффолде
      rc_blast = rc_blast.groupby(["Gene"]).agg({'readcounts': 'sum','Contig':'first','sseqid':'first','pident':'mean'}).reset_index()
      #Читаем результат Kraken
      kraken_names = pd.read_csv(f'Kraken-results/Kraken_tables/Kraken_table_file_{patient}.txt', sep=",", nrows=0).columns.tolist()
      num_cols = len(kraken_names)
      dtype_dict = {kraken_names[0] : int} # First column is int
      for j in range(1, num_cols):
          dtype_dict[kraken_names[j]] = str
          
      kraken = pd.read_csv(f'Kraken-results/Kraken_tables/Kraken_table_file_{patient}.txt', sep=",",dtype=dtype_dict)
      #Объединяем с Kr по имени скаффолда 
      rc_blast_kraken = kraken.merge(rc_blast, on='Contig')
      rc_blast_kraken['Patient'] = patient
      rc_blast_kraken.drop('Contig',axis = 1,inplace = True)
      dfs.append(rc_blast_kraken)
    
    
result = pd.concat(dfs)

#форматируем название таксона и добавляем функции генов из данных, заложенных в sseqid
result = result(axis=1, how='all')
result = result[['d','p','s','ID','Patient','Gene','pident','readcounts','sseqid']].sort_values(['d','p','s','ID','Patient','Gene','pident','readcounts','sseqid']).reset_index(drop = True)
result.to_csv('Signature-pairs/taxon-gene-example.csv',index = False)
```

Полученная таблица имеет вид:

```{r}
#| echo: False
table <- read.csv("Signature-pairs/taxon-gene-full.csv")
knitr::kable(table[1:10, ], align = "c")
```

## Сделаем сводную таблицу с генами, которые принадлежат патогенным видам, согласно [списку патогенов CZ ID Pathogen List](https://czid.org/pathogen_list)

```{python}
pwd_hc = pd.read_csv('Signature-pairs/taxon-gene-full.csv')

pwd_hc['readcounts'] = pwd_hc['readcounts'].astype(float)
pwd_hc['pident'] = pwd_hc['pident'].astype(float)

pwd_hc['Gene_info'] = pwd_hc['sseqid'].apply(lambda x: (x.split('_[')[0]))

#Оставми только те гены, которые принадлежат патогенам
IDs = pd.read_csv('Obtained-data/Pathogen-data/CZ_ID_pathogen_list_2024.tsv', sep='\t')
IDs['taxon_name'] = IDs['taxon_name'].str.replace(' ', '_')
pwd_hc = pwd_hc.loc[pwd_hc['s'].isin(IDs['taxon_name'])]

df = pwd_hc.pivot_table(columns='Patient', index=['p','s','Gene_info', 'pident'], values='readcounts', aggfunc='sum').fillna(0).reset_index()
columns = df.columns
df = df.sort_values(['p','s','Gene_info', 'pident']).reset_index(drop = True)

aggregation_dict = {'pident': 'mean'}
for col in list(columns[4:]):
    aggregation_dict[col] = 'sum' 

df = df.groupby(['p','s','Gene_info']).agg(aggregation_dict).reset_index()
df['Pair'] = df['p']+ '$' + df['s'] +'$'+ df['Gene_info'] +'$'+df['pident'].astype(str)
df = df.drop(['p','s','Gene_info','pident'], axis = 1)

df = df[['Pair']+sorted(columns[4:], key=lambda x: (x.split('_')[0],int(x.split('_')[1])))]




df.to_csv('Signature-pairs/taxon-gene-pivot.csv', index = False)

```

В ней гены по строчкам, а пациенты по столбцам

Для визуализации распределения числа встречаемости сигнатурных пар в группах PD и HC был построен рисунок ниже. По оси x отложены пациенты группы HC, по оси y – пациенты группы PD. Серым цветом выделены гены в сигнатурных парах с низкой частотой встречаемости (\<=2 ненулевых значения в обеих группах). Эти гены были исключены из дальнейшего анализа, так как их включение могло бы сделать FDR-коррекцию излишне консервативной. На рисунке видно, что распределение данных характеризуется избытком нулей, что свидетельствует о ненормальности распределения.

```{python}
from collections import defaultdict
import matplotlib.pyplot as plt
import numpy as np

def plot_zeroes_jitter(data, treshold = 5):
    '''data has genes as index, patients in columns'''
    data = data.T
    data['status'] = data.index.map(lambda x: 0 if x[0] in ('h', 'H') else 1)
    gene_names = [col for col in data.columns if col != 'status']
    gene_data = {}  # Store data for each gene
    coordinate_counts = defaultdict(int)

    for gene in tqdm(gene_names, desc="Processing Genes"):
        healthy = data[data['status'] == 0][gene]
        sick = data[data['status'] == 1][gene]
        healthy = pd.to_numeric(healthy, errors='coerce').dropna()  # Convert to numeric, handling errors
        sick = pd.to_numeric(sick, errors='coerce').dropna()
        non_zero_count = ((healthy != 0).sum(), (sick != 0).sum())

        gene_data[gene] = {'healthy_zeros': non_zero_count[0],
                               'sick_zeros': non_zero_count[1],
                              }

        coordinate_counts[non_zero_count] += 1
        

    fig, ax = plt.subplots(figsize=(6, 6))# Use seaborn's styling for better visuals
    ax = plt.gca() # Access the current axes
    # Add a subtle background color
    fig.patch.set_facecolor('whitesmoke')  # Very light gray
    plt.xlabel('Число ненулевых значений в образцах здоровых (HC)', fontsize=12)
    plt.ylabel('ЧИсло ненулевых значений в образцах больных (PD)', fontsize=12)

    # Shaded Regions with more distinct colors
    plt.fill_between(np.linspace(-1, treshold, 100), -1, treshold, color='lightgrey', alpha=0.3,
                     label=f'Гены с < {treshold} ненулевых значений в обеих группах')  # Red area

    jitter_amount = 0.3
    x_values = []  # Store x coordinates
    y_values = []
    for gene, data in tqdm(gene_data.items(), desc="Plotting Genes"):
        x = data['healthy_zeros'] + np.random.uniform(-jitter_amount, jitter_amount)
        y = data['sick_zeros'] + np.random.uniform(-jitter_amount, jitter_amount)
        plt.scatter(x, y, s=30, c='dodgerblue', marker='o', alpha=0.7, edgecolors='navy', linewidths=0.5)
        x_values.append(x)
        y_values.append(y)
    # Annotations with adjusted positioning
    for coordinates, count in tqdm(coordinate_counts.items(), desc='Annotating Counts'):
        healthy_zeros, sick_zeros = coordinates
        plt.text(healthy_zeros, sick_zeros, str(count), ha='center', va='center', color='black', fontsize=8, zorder=3,
                 bbox=dict(facecolor='white', alpha=0.5, edgecolor='none')) #Add background so that labels are easier to see.

    plt.xlim(-1, max(x_values)+1)
    plt.ylim(-1, max(y_values)+2)

    # Refine Grid Lines
    ax.grid(True, linestyle='--',alpha=0.5)
    #ax.grid(False, which='minor')  # Remove minor gridlines for cleaner look

    # Ticks with more distinct styling
    plt.xticks(np.arange(0, max(x_values)+1, 1), fontsize=10)
    plt.yticks(np.arange(0, max(y_values)+2, 1), fontsize=10)

    # Legend Improvements
    plt.legend(loc='upper left', frameon=True, framealpha = 0.8, edgecolor='black')  # Add frame for better visibility
    plt.tight_layout()
    plt.show()
```

```{python}
#| label: fig-plot_zeroes_jitter
norm_com = pd.read_csv('Signature-pairs/taxon-gene-pivot.csv', index_col = 0)
plot_zeroes_jitter(norm_com,treshold = 3)
```

Поэтому обрежем эту область и запишем в новый датафрейм

```{python}
data = pd.read_csv('Signature-pairs/taxon-gene-pivot.csv', index_col = 0)

hc_cols = [col for col in data.columns if col.startswith('HC')]
pd_cols = [col for col in data.columns if  col.startswith('PD')]

healthy_nonzero = (data[hc_cols] > 0).sum(axis=1)
sick_nonzero = (data[pd_cols] > 0).sum(axis=1)

mask = ( (sick_nonzero > 3) |  (healthy_nonzero > 3))
data = data[mask]

cols_to_drop = data.columns[(data == 0.0).all()].tolist()

# Drop the identified columns
df_cleaned = data.drop(columns=cols_to_drop)

df_cleaned.to_csv('Signature-pairs/cut_taxon-gene-pivot.csv')

```

```{python}
norm_com = pd.read_csv('Signature-pairs/cut_taxon-gene-pivot.csv', index_col = 0)
plot_zeroes_jitter(norm_com,treshold = 3)
```

## TMM - нормализация ридкаунтов

Перед проведением сравнительного анализа представленности генов патогенности была выполнена TMM (Trimmed Mean of M-values) - коррекция числа прочтений с использованием пакета edgeR. Необходимость данной коррекции обусловлена влиянием различных систематических ошибок на результаты анализа, в частности, различиями в глубине секвенирования образцов, не связанными с биологическими особенностями.

```{r}
#| eval: false
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")

BiocManager::install("edgeR")
```

```{r}
library(edgeR)
```

```{r}
# Load the data

filtered_genes <- read.csv('Signature-pairs/cut_taxon-gene-pivot.csv',  header = TRUE, row.names = 1)

# Create the DGEList Object
groups <- gsub("_.*", "", colnames(filtered_genes))


q <- DGEList(counts=filtered_genes, group=factor(groups))

# Calculate logCPM before normalization
logCPM_before <- cpm(q, log=TRUE, prior.count = 1)

# Perform TMM normalization
q <- calcNormFactors(q)

# Calculate logCPM after normalization
logCPM_after <- cpm(q, log=TRUE, prior.count = 1)

# --- Improved Boxplot Visualization ---

# 1. Reshape the data for boxplotting using melt or pivot_longer

library(reshape2)  # or use tidyr package for pivot_longer

# Before normalization
logCPM_before_melted <- melt(logCPM_before)  # Converts to long format
colnames(logCPM_before_melted) <- c("Gene", "Sample", "LogCPM")  # Rename columns

# After normalization
logCPM_after_melted <- melt(logCPM_after)
colnames(logCPM_after_melted) <- c("Gene", "Sample", "LogCPM")


# 2. Create boxplots using ggplot2 for better aesthetics and control
library(ggplot2)


# Before Normalization Density Plot
ggplot(logCPM_before_melted, aes(x = LogCPM, fill = Sample)) +
  geom_density(alpha = 0.5) +  # Density plot with transparency
  ggtitle("Log-CPM Density Before Normalization") +
  xlab("Log-CPM") +
  ylab("Density") +
  theme_bw() +
  theme(legend.position = "right") # Move legend for better readability


# After Normalization Density Plot
ggplot(logCPM_after_melted, aes(x = LogCPM, fill = Sample)) +
  geom_density(alpha = 0.5) +  # Density plot with transparency
  ggtitle("Log-CPM Density After Normalization") +
  xlab("Log-CPM") +
  ylab("Density") +
  theme_bw() +
  theme(legend.position = "right")

# Save normalized counts
normalized_counts <- cpm(q)
write.csv(normalized_counts, file = 'Signature-pairs/norm_cut_taxon-gene-pivot.csv')
```

```{r}
q$samples
```

Результаты TMM-коррекции представлены на рисунке ниже, где по оси X отложены образцы, а по оси Y показано распределение логарифма числа прочтений генов в сигнатурных парах, обнаруженных в образцах. Визуально видно, что после применения TMM-коррекции распределения числа прочтений становятся более сопоставимыми между образцами, что позволяет проводить корректный сравнительный анализ.

```{python}
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
import numpy as np
import matplotlib.patches as mpatches

def plot_jittered_log_readcount_distribution(df, y_jitter_scale=0.1, boxplot_width=0.4, title="Распределение числа прочтений по образцам (Violin Plot)"):

    # Extract the sample names from the column names, excluding the first (Gene name)
    sample_names = df.columns.tolist() # Corrected this line
    df.index.name = 'Gene name and function'
    df = df.reset_index()

    # Melt the DataFrame to long format for easier plotting
    df_melted = pd.melt(df, id_vars=['Gene name and function'], value_vars=sample_names, var_name='Sample', value_name='Readcounts')

    # Apply a log transformation to the readcounts (using log1p to handle 0 values)
    df_melted['LogReadcounts'] = np.log1p(df_melted['Readcounts'])
    
    n_pd = sum(1 for s in sample_names if s.startswith('PD_'))
    n_hc = sum(1 for s in sample_names if s.startswith('HC_'))

    # Define colors based on sample name and low count status
    def get_color(sample):
        if sample.startswith('PD_'):
            return 'red'
        elif sample.startswith('HC_'):
            return 'green'
        else:
            return 'blue'  # Default color

    sample_colors = [get_color(sample) for sample in df_melted['Sample']]
    
    

    # --- Improved Plotting ---
    plt.figure(figsize=(10, 5))  # Increased figure size for better readability
    ax = sns.violinplot(x='Sample', y='LogReadcounts', data=df_melted, inner='quartile',  linewidth=0.8,  palette=[get_color(s) for s in df_melted['Sample'].unique()]) 

    # Customize the plot
    plt.title(title, fontweight='bold')  # Added fontweight for emphasis
    plt.xlabel('Образец')
    plt.ylabel('Log(Число прочтений + 1)')
    plt.xticks(rotation=75, ha='right')  # Reduced rotation to 45 degrees
    #plt.yticks(fontsize=14)
    plt.grid(axis='y', linestyle='--', alpha=0.5)
    sns.despine(left=True)  # Keep only the bottom and right spines

    # Add some space at the bottom for the x-axis labels
    plt.subplots_adjust(bottom=0.2)
    
    
    # --- Add Legend ---
    legend_elements = [
        mpatches.Patch(color='green', label=f'37% пациентов группы HC ({n_hc}/38)'),
        mpatches.Patch(color='red', label=f'58% пациентов группы PD ({n_pd}/36)')
        #mpatches.Patch(color='grey', label=f'Samples with < 5 non-zero readcounts, n={n_low_count} (out of 74)'),
        
    ]
    plt.legend(handles=legend_elements, loc='upper right')
    
    

    plt.tight_layout()
    
    plt.show()
    

```

```{python}
con = pd.read_csv('Signature-pairs/cut_taxon-gene-pivot.csv', index_col = 0)

norm_com = pd.read_csv('Signature-pairs/norm_cut_taxon-gene-pivot.csv', index_col = 0)
plot_jittered_log_readcount_distribution(con, title="Распределение числа прочтений по образцам до TMM")

plot_jittered_log_readcount_distribution(norm_com, title="Распределение числа прочтений по образцам после TMM")
```

```{python}
import pandas as pd
import matplotlib.pyplot as plt
import numpy as np
import seaborn as sns  # For styling
from matplotlib import ticker

# Load the data, specifying tab separation and index column
df = pd.read_csv('Signature-pairs/norm_cut_taxon-gene-pivot.csv', index_col = 0)
# Calculate Mean and Variance for Each Gene
df['mean'] = df.mean(axis=1)
df['variance'] = df.var(axis=1)


# Set a visually appealing style
sns.set_style("whitegrid")  # "whitegrid", "darkgrid", "white", "dark", "ticks"

# Define a visually appealing color palette
# You can choose from: "deep", "muted", "pastel", "bright", "dark", "colorblind"
sns.set_palette("bright")

# Create the figure and axes
fig, ax = plt.subplots(figsize=(8, 6))  # Adjusted figure size for better aesthetics

# Scatter plot with improved aesthetics
scatter = ax.scatter(df['mean'], df['variance'], s=20, c='dodgerblue', marker='o', alpha=0.7, edgecolors='navy', linewidths=0.5, label='Gene Expression Data') # Reduced point size

# Fit a polynomial of degree 2
coeffs = np.polyfit(df['mean'], df['variance'], 2)
poly = np.poly1d(coeffs)

# Generate x values for plotting the polynomial
x = np.linspace(df['mean'].min(), df['mean'].max(), 200)  # Increased points for smoother curve
y = poly(x)

# Plot the fitted polynomial with a different color and linewidth
ax.plot(x, y, color='darkred', linewidth=2, label=r'Fitted Polynomial: $y = {:.1f}x^2  {:.2} \cdot 10^4 x + {:.2} \cdot 10^9 $'.format(coeffs[0], coeffs[1]/10000, coeffs[2]/10**9))  # Increased linewidth and scientific notation

# Improved labels and title
ax.set_xlabel('Mean Gene Expression Level (Normalized)', fontsize=12)
ax.set_ylabel('Variance of Expression (Normalized)', fontsize=12)
ax.set_title('Variance vs. Mean Expression for Genes (TMM Normalized Data)', fontsize=14, fontweight='bold')

# Customize grid lines
ax.grid(True, linestyle='--', alpha=0.5)

# Add a subtle background color
fig.patch.set_facecolor('whitesmoke')  # Very light gray

# Add a legend with a better placement and frame
ax.legend(loc='upper left', frameon=True, framealpha=0.8, edgecolor='black')

# Adjust the tick label size
ax.tick_params(axis='both', which='major', labelsize=10)

# Format axis ticks to use scientific notation when appropriate
formatter = ticker.ScalarFormatter(useMathText=True)
formatter.set_scientific(True)
formatter.set_powerlimits((-1,1))
ax.xaxis.set_major_formatter(formatter)
ax.yaxis.set_major_formatter(formatter)

# Remove top and right spines (optional)
ax.spines['top'].set_visible(True)
ax.spines['right'].set_visible(True)

# Tight layout to prevent labels from overlapping
plt.tight_layout()

# Show the plot
plt.show()
```

Следует отметить, что полученные ридкаунты отражают потенциальную кодирующую способность участков генома, но не свидетельствуют о фактической экспрессии генов патогенности. В данном контексте более важным является наличие или отсутствие этих генов в патогенных микроорганизмах. При отсутствии гена, учитывая фрагментированность метагеномных данных, можно предположить, что патоген не экспрессирует данный ген. Поэтому первым шагом был проведен анализ различий в частоте встречаемости генов патогенности между группами.

## Точный тест Фишера

Для всех сигнатурных пар вне серой зоны на рисунке выше был применен точный тест Фишера с использованием функции fisher_exact библиотеки scipy.stats в Python. Этот тест предназначен для проверки гипотезы о независимости двух групп по двум переменным (в данном случае, по числу нулевых и ненулевых значений), с использованием гипергеометрического распределения для значений в ячейках таблицы 2x2. Тест Фишера, как и хи-квадрат, является непараметрическим и особенно полезен для анализа малых или разреженных данных, где применение хи-квадрат может быть неточным.

```{python}
import pandas as pd
import numpy as np
from scipy.stats import chi2_contingency, fisher_exact
from statsmodels.stats.multitest import multipletests

data = pd.read_csv('Signature-pairs/norm_cut_taxon-gene-pivot.csv', index_col = 0)

hc_cols = [col for col in data.columns if col.startswith('HC')]
pd_cols = [col for col in data.columns if  col.startswith('PD')]

# Count non-zero and zero values
def count_values(row, cols):
    non_zero = (row[cols] != 0).sum()
    zero = len(cols) - non_zero
    return non_zero, zero

results = []
for gene, row in data.iterrows():
    hc_non_zero, hc_zero = count_values(row, hc_cols)
    pd_non_zero, pd_zero = count_values(row, pd_cols)
    table = [[hc_non_zero, pd_non_zero], [hc_zero, pd_zero]]
    
    # Choose test
    try:
        _, p_chi2, _, expected = chi2_contingency(table)
        if expected.min() < 5:
            type = 'Fisher'
            _, p_value = fisher_exact(table)
        else:
            type = 'Chi'
            p_value = p_chi2
    except:
        p_value = np.nan
    
    results.append({
        'Gene': gene,
        'HCnon_zero': hc_non_zero,
        'PDnon_zero': pd_non_zero,
        'p_value': p_value,
        'test': type
    })

# Convert to DataFrame and adjust p-values
results_df = pd.DataFrame(results)
results_df['p_adj'] = multipletests(results_df['p_value'], method='fdr_bh')[1]

# Save significant genes
sig_genes = results_df[results_df['p_adj'] < 0.1]

sig_genes

```

Сигнатурные пары, для которых FDR \< 0.1, выделены красным цветом на рисунке 14. Их общее количество составило 59.

## Изобразим полученные результаты на Sankey-диаграммах

```{python}
import pandas as pd
import plotly.graph_objects as go
from plotly.colors import qualitative
```

```{python}
#| output: false

def get_function_group_colors(df, level_col):
        df['Group'] = df['Patient'].str.extract(r'^(HC|PD)')[0]
        level_groups = df.groupby([level_col, 'Group'])['readcounts'].sum().unstack().fillna(0)
        level_groups = level_groups > 0
        
        level_colors = {}
        for level in level_groups.index:
            in_hc = level_groups.loc[level, 'HC'] if 'HC' in level_groups.columns else False
            in_pd = level_groups.loc[level, 'PD'] if 'PD' in level_groups.columns else False
            
            if in_hc and in_pd:
                level_colors[level] = "#FFB300"  # Желтый (оба)
            elif in_hc:
                level_colors[level] = '#2ECC71'  # Зеленый (HC)
            elif in_pd:
                level_colors[level] = '#E74C3C'  # Красный (PD)
            else:
                level_colors[level] = '#AAAAAA'
        
        return level_colors


pwd_hc = pd.read_csv('Signature-pairs/taxon-gene-full.csv')[['Gene class', 'Gene', 'p','s','Gene function', 'Patient', 'readcounts']]
IDs = pd.read_csv('Obtained-data/Pathogen-data/CZ_ID_pathogen_list_2024.tsv', sep='\t').iloc[:252]
IDs['taxon_name'] = IDs['taxon_name'].str.replace(' ', '_')
pathogen_df = pwd_hc.copy()
pathogen_df = pathogen_df[pwd_hc['s'].isin(IDs['taxon_name'])]
# Добавляем группу (HC/PD)
pathogen_df['Group'] = pathogen_df['Patient'].apply(lambda x: x.split('_')[0])


# Получаем все уникальные категории
all_groups = sorted(pathogen_df['Group'].unique())
all_pathogen_phyla = sorted(pathogen_df['p'].dropna().unique())
all_pathogen_species = sorted(pathogen_df['s'].unique())
pathogen_gene_funcs = sorted(pathogen_df['Gene class'].unique())

group_colors = {'HC': 'rgba(0, 158, 115, 0.8)', 'PD': 'rgba(255, 0, 0, 0.8)'}  
color_scale = qualitative.Plotly  # Basic Color Scale
pathogen_func_colors = {func: color_scale[i % len(color_scale)] for i, func in enumerate(pathogen_gene_funcs)}

nodes = all_groups + all_pathogen_phyla + all_pathogen_species + pathogen_gene_funcs
node_indices = {node: i for i, node in enumerate(nodes)}

# Build Links
sources, targets, values, link_colors = [], [], [], []

# Groups -> Phyla
group_phylum_links = pathogen_df.groupby(['Group', 'p'])['readcounts'].sum().reset_index()
for _, row in group_phylum_links.iterrows():
    sources.append(node_indices[row['Group']])
    targets.append(node_indices[row['p']])
    values.append(row['readcounts'])
    link_colors.append(group_colors[row['Group']])

# Phyla -> Species
phylum_species_links = pathogen_df.groupby(['p', 's', 'Group'])['readcounts'].sum().reset_index() 
for _, row in phylum_species_links.iterrows():
    sources.append(node_indices[row['p']])
    targets.append(node_indices[row['s']])
    values.append(row['readcounts'])
    link_colors.append(group_colors[row['Group']])  

# Species -> Gene Functions
s_gene_links = pathogen_df.groupby(['s', 'Gene class', 'Group'])['readcounts'].sum().reset_index() 
for _, row in s_gene_links.iterrows():
    sources.append(node_indices[row['s']])
    targets.append(node_indices[row['Gene class']])
    values.append(row['readcounts'])
    link_colors.append(group_colors[row['Group']])


# Diagram Layout

node_colors = (
        [group_colors[g] for g in all_groups] +
        [get_function_group_colors(pathogen_df, 'p').get(phylum, '#AAAAAA') for phylum in all_pathogen_phyla] +
        [get_function_group_colors(pathogen_df, 's').get(species, '#AAAAAA') for species in all_pathogen_species] +
        [get_function_group_colors(pathogen_df, 'Gene class').get(func, '#AAAAAA') for func in pathogen_gene_funcs]
    )
link = dict(source=sources, target=targets, value=values, color=link_colors)
node = dict(pad=15, thickness=20, line=dict(color="black", width=0.5), label=nodes, color =node_colors)
sankey = go.Sankey(link=link, node=node)
fig = go.Figure(sankey)

fig.update_layout(
    title={
        'text': "<b>Распределение генов патогенов по таксономическим группам</b>",
        'y': 0.95, 'x': 0.5, 'xanchor': 'center', 'yanchor': 'top',
        'font': {'size': 20, 'family': 'Palatino Linotype'}
    },
    font=dict(family="Palatino Linotype", size=16, color="black"),  
    height=800, width=1400,  
    margin=dict(t=100, b=80, l=40, r=40),  
    plot_bgcolor='rgba(255,255,255,0.9)', paper_bgcolor='rgba(255,255,255,0.9)',
    hovermode='x unified',
    annotations=[
        dict(x=0, y=1.05, xref='paper', yref='paper', align='left',
             showarrow=False, bordercolor='rgba(0,0,0,0.2)', borderwidth=1, borderpad=4,
             bgcolor='rgba(255,255,255,0.8)',
             font=dict(size=16, family='Palatino Linotype'),
             text="<b>Цвета связей:</b><br>"
                     "<span style='color:rgba(0, 158, 115, 0.8)'>■ От здоровых пациентов (HC)</span><br>"
                     "<span style='color:rgba(255, 5, 0, 0.8)'>■ От пациентов с депрессией (PD)</span>"),
        dict(x=0.5, y=-0.1, xref='paper', yref='paper', showarrow=False,
             font=dict(size=16, family='Palatino Linotype'),
             text="<b>Уровни анализа:</b> Группы пациентов → Род патогена → Вид патогена → Функции генов")  
    ]
)
fig.write_html("images/sankey_diagram.html")
```

Изобразим только патогенные виды:

```{r}
#| echo: false
#| error: FALSE
library(htmltools)
includeHTML("images/sankey_diagram.html")
```

```{python}
#| eval: false
# Load and prepare data
data = pd.read_excel('Signature-pairs/2full_genes.xlsx', index_col = 0)[['Класс-гена','Описание','Функция','Название кластера','Название гена']]#
pwd_hc = pd.read_csv('Signature-pairs/taxon-gene-full.csv')[['Gene class', 'Gene', 'p','s', 's1','Gene function', 'Patient', 'readcounts']]
IDs = pd.read_csv('Obtained-data/Pathogen-data/CZ_ID_pathogen_list_2024.tsv', sep='\t').iloc[:252]
IDs['taxon_name'] = IDs['taxon_name'].str.replace(' ', '_')

pwd_hc = pwd_hc.loc[~pwd_hc['s'].isna()]
pwd_hc = pwd_hc.loc[pwd_hc['s'].apply(lambda x: len(x.split('_')) == 2)]
#pwd_hc = pwd_hc.loc[pwd_hc['s'].isin(IDs['taxon_name'])]


pwd_hc = pwd_hc.merge(data, left_on = 'Gene', right_on = 'Название гена')[['Класс-гена','Описание','Функция','Название кластера','Название гена','Patient','p','s','readcounts','Gene class']]
# Добавляем группу (HC/PD)
pwd_hc['Group'] = pwd_hc['Patient'].apply(lambda x: x.split('_')[0])

# Add pathogen status to main data
pwd_hc['Pathogen'] = pwd_hc['s'].isin(IDs['taxon_name'])

# Split data
pathogen_df = pwd_hc.loc[pwd_hc['Pathogen']].copy()
n_pat_df = pwd_hc.loc[~pwd_hc['Pathogen']].copy()

# Get all unique categories
all_groups = sorted(set(pwd_hc['Group'].unique()))
all_pathogen_species = sorted(pathogen_df['s'].unique())
all_nonpathogen_species = sorted(n_pat_df['s'].unique())
all_pathogen_phyla = sorted(pathogen_df['p'].unique())
all_nonpathogen_phyla = sorted(n_pat_df['p'].unique())

# Find gene functions present in both sides
funcs = set(pwd_hc['Gene class'].unique())
pathogen_gene_funcs = set(pathogen_df['Gene class'].unique())
nonpathogen_gene_funcs = set(n_pat_df['Gene class'].unique())
common_gene_funcs = sorted(pathogen_gene_funcs & nonpathogen_gene_funcs)
pathogen_only_funcs = sorted(pathogen_gene_funcs - nonpathogen_gene_funcs)
nonpathogen_only_funcs = sorted(nonpathogen_gene_funcs - pathogen_gene_funcs)

# Initialize lists to store the data for the Sankey diagram
sources = []
targets = []
values = []
link_colors = []


# Create variables for "not present" hierarchy.
pathogen_only_not_present_species = "Not present in nonpathogens"
pathogen_only_not_present_phyla = " "
pathogen_only_not_present_groups = "  "


# Create a dictionary to map nodes (species, phyla, groups, gene functions) to indices
nodes = ([f"{group} (Pathogens)" for group in all_groups] +                  # Группы пациентов (0-1)
    [f"{phylum} (P)" for phylum in all_pathogen_phyla] +        # Типы (2-N)
    all_pathogen_species +  
    common_gene_funcs+pathogen_only_funcs+nonpathogen_only_funcs +
    all_nonpathogen_species +
    [f"{phylum} (NP)" for phylum in all_nonpathogen_phyla] +
    [f"{group} (Non-Pathogens)" for group in all_groups] + [pathogen_only_not_present_species,pathogen_only_not_present_phyla,pathogen_only_not_present_groups])



node_indices = {node: i for i, node in enumerate(nodes)}



# Create color mappings
# Groups
group_colors = {'HC': 'rgba(0, 158, 115, 0.8)', 'PD': 'rgba(255, 0, 0, 0.8)'}  

# Function to determine group presence and assign colors
def get_group_presence_colors(df, level_col):
    # Extract group from Patient column (HC_... or PD_...)
    df['Group'] = df['Patient'].str.extract(r'^(HC|PD)')[0]
    
    level_groups = df.groupby([level_col, 'Group'])['readcounts'].sum().unstack().fillna(0)
    level_groups = level_groups > 0  # Convert to boolean presence/absence
    
    level_colors = {}
    for level in level_groups.index:
        in_hc = level_groups.loc[level, 'HC'] if 'HC' in level_groups.columns else False
        in_pd = level_groups.loc[level, 'PD'] if 'PD' in level_groups.columns else False
        
        if in_hc and in_pd:
            level_colors[level] = "#FFB300"  # Yellow (both)
        elif in_hc:
            level_colors[level] = '#2ECC71'  # Green (HC only)
        elif in_pd:
            level_colors[level] = '#E74C3C'  # Red (PD only)
        else:
            level_colors[level] = '#AAAAAA'  # Grey (shouldn't happen)
    
    return level_colors

# Get color mappings for species and phyla
pathogen_species_colors = get_group_presence_colors(pathogen_df, 's')
nonpathogen_species_colors = get_group_presence_colors(n_pat_df, 's')
pathogen_phyla_colors = get_group_presence_colors(pathogen_df, 'p')
nonpathogen_phyla_colors = get_group_presence_colors(n_pat_df, 'p')



# 1. Left Groups → Phyla (Pathogen side)
group_phylum_links = pathogen_df.groupby(['Group', 'p'])['readcounts'].sum().reset_index()
for _, row in group_phylum_links.iterrows():
    sources.append(node_indices[f"{row['Group']} (Pathogens)"])
    targets.append(node_indices[f"{row['p']} (P)"])
    values.append(max(10,row['readcounts']))
    link_colors.append(group_colors[row['Group']])  # Red for pathogen side

# 2. Phyla → Pathogens
phylum_species_links = pathogen_df.groupby(['p', 's','Group'])['readcounts'].sum().reset_index()
for _, row in phylum_species_links.iterrows():
    sources.append(node_indices[f"{row['p']} (P)"])
    targets.append(node_indices[row['s']])
    values.append(max(17000,row['readcounts']))
    link_colors.append(group_colors[row['Group']])  # Red for pathogen side

# 3. Pathogens → Gene Functions
species_gene_links = pathogen_df.groupby(['s', 'Gene class','Group'])['readcounts'].sum().reset_index()
for _, row in species_gene_links.iterrows():
    if row['Gene class'] in common_gene_funcs:
        targets.append(node_indices[row['Gene class']])
    elif row['Gene class'] in pathogen_only_funcs:
        targets.append(node_indices[row['Gene class']])
    else:
        continue
        
    sources.append(node_indices[row['s']])
    values.append(row['readcounts'])
    link_colors.append(group_colors[row['Group']])  # Red for pathogen side

# 4. For pathogen-only functions: connect to "not present" hierarchy
# For pathogen-only functions: 
for func in pathogen_only_funcs:
    # First get the total readcounts for this function across all pathogens
    func_readcounts = pathogen_df[pathogen_df['Gene class'] == func]['readcounts'].sum()
    
    
    # Then connect to "not present" hierarchy using proportional readcounts
    # Function → "not present in non-path species"
    sources.append(node_indices[func])
    targets.append(node_indices[pathogen_only_not_present_species])
    values.append(func_readcounts)  # Reduced for visibility
    link_colors.append('rgba(150, 150, 150, 0.4)')
    
    # "not present in non-path species" → "not present in non-path phyla"
    sources.append(node_indices[pathogen_only_not_present_species])
    targets.append(node_indices[pathogen_only_not_present_phyla])
    values.append(func_readcounts)  # Further reduced
    link_colors.append('rgba(150, 150, 150, 0.4)')
    
    # "not present in non-path phyla" → "not present in non-path groups"
    sources.append(node_indices[pathogen_only_not_present_phyla])
    targets.append(node_indices[pathogen_only_not_present_groups])
    values.append(func_readcounts)  # Smallest value
    link_colors.append('rgba(150, 150, 150, 0.4)')


# 5. Common gene functions → Non-pathogens
gene_species_links = n_pat_df.groupby(['Gene class', 's','Group'])['readcounts'].sum().reset_index()
for _, row in gene_species_links.iterrows():
    if row['Gene class'] in common_gene_funcs:
        sources.append(node_indices[row['Gene class']])
        targets.append(node_indices[row['s']])
        values.append(max(15000,row['readcounts']))
        link_colors.append(group_colors[row['Group']])  # Grey for middle links

# 6. Non-pathogens → Phyla (Non-pathogen side)
species_phylum_links = n_pat_df.groupby(['s', 'p','Group'])['readcounts'].sum().reset_index()
for _, row in species_phylum_links.iterrows():
    sources.append(node_indices[row['s']])
    targets.append(node_indices[f"{row['p']} (NP)"])
    values.append(row['readcounts'])
    link_colors.append(group_colors[row['Group']])  # Green for non-pathogen side

# 7. Phyla → Right Groups
phylum_group_links = n_pat_df.groupby(['p', 'Group'])['readcounts'].sum().reset_index()
for _, row in phylum_group_links.iterrows():
    sources.append(node_indices[f"{row['p']} (NP)"])
    targets.append(node_indices[f"{row['Group']} (Non-Pathogens)"])
    values.append(row['readcounts'])
    link_colors.append(group_colors[row['Group']])  # Green for non-pathogen side

# 8. For nonpathogen-only functions: 
for func in nonpathogen_only_funcs:

    sources.append(node_indices[nonpathogen_only_not_present_groups])
    targets.append(node_indices[nonpathogen_only_not_present_phyla])
    values.append(row['readcounts'])
    link_colors.append('rgba(150, 150, 150, 0.4)')
    
    # "not present in path phyla" → "not present in path species"
    sources.append(node_indices[nonpathogen_only_not_present_phyla])
    targets.append(node_indices[nonpathogen_only_not_present_species])
    values.append(row['readcounts'])
    link_colors.append('rgba(150, 150, 150, 0.4)')
    
    # "not present in path species" → function
    sources.append(node_indices[nonpathogen_only_not_present_species])
    targets.append(node_indices[func])
    values.append(row['readcounts'])
    link_colors.append('rgba(150, 150, 150, 0.4)')

for i, source in enumerate(sources):
    source_node = nodes[source]
    target_node = nodes[targets[i]]
    
    # Determine color based on the side the link originates from
    if source_node.endswith('(Pathogens)') or source_node.endswith('(P)') or source_node in all_pathogen_species:
        # Left (pathogen) side - use red
        link_colors.append('rgba(231, 76, 60, 0.6)')  # Red with transparency
    elif target_node.endswith('(Non-Pathogens)') or target_node.endswith('(NP)') or target_node in all_nonpathogen_species:
        # Right (non-pathogen) side - use green
        link_colors.append('rgba(46, 204, 113, 0.6)')  # Green with transparency
    else:
        # Middle connections - use grey
        link_colors.append('rgba(150, 150, 150, 0.4)')  # Grey with transparency
        
        
# Diagram Layout


link = dict(source=sources, target=targets, value=values, color=link_colors)
node = dict(pad=15, thickness=20, line=dict(color="black", width=0.5), label=nodes)
sankey = go.Sankey(link=link, node=node)


# Create the Sankey diagram
fig = go.Figure(go.Sankey(
    arrangement="snap",
    node=dict(
        pad=30,
        thickness=20,
        line=dict(color="black", width=0.5),
        label=nodes,
        #color=node_colors,
    ),
    link=dict(
        source=sources,
        target=targets,
        value=values,
        color=link_colors
    )
))
fig.update_layout(
    font_size=12,
    height=1000,
    width=1800,
    annotations=[
        dict(
            x=0.9,
            y=1.1,
            xref='paper',
            yref='paper',
            text=
                 "<b>Link Colors:</b><br>"
                 "<span style='color:rgba(255, 0, 0, 0.8)'>■ from PD</span><br>"
                 "<span style='color:rgba(0, 158, 115, 0.8)'>■ from HC</span><br>",

            showarrow=False,
            align='left',
            bordercolor='black',
            borderwidth=1,
            borderpad=4,
            bgcolor='white'
        )
    ]
)

#fig.show()
fig.write_html("images/sankey_diagram_with_HC.html")

```

Изобразим слева - патогенные виды, а справа - непатогенные.

```{r}
#| echo: false
#| error: FALSE
library(htmltools)
includeHTML("images/sankey_diagram_with_HC.html")
```